import { useEffect, useState } from "react";
import { MessageCircle, Plus, Users } from "lucide-react";
export default function RelationshipsPage() {
  const [people, setPeople] = useState<any[]>([]); const [selected, setSelected] = useState<any>(null); const [intel, setIntel] = useState<any>(null); const [text, setText] = useState("");
  const load = async () => { const r = await fetch("/api/relationships/people", { cache: "no-store" }); if (r.ok) setPeople(await r.json()); };
  useEffect(() => { void load(); }, []);
  const open = async (person: any) => { setSelected(person); const r = await fetch(`/api/relationships/people/${person.id}/intelligence`, { cache: "no-store" }); if (r.ok) setIntel(await r.json()); };
  const addPromise = async () => { if (!text || !selected) return; await fetch(`/api/relationships/people/${selected.id}/promises`, { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ statement: text }) }); setText(""); await open(selected); };
  return <div className="mx-auto max-w-[1250px]"><p className="lee-label text-primary">Relationship intelligence</p><h1 className="mt-2 text-3xl font-semibold">People</h1><p className="mt-2 text-sm text-muted-foreground">Promises, questions, interaction momentum, and relationship health remain grounded in recorded evidence.</p><div className="mt-5 grid gap-5 lg:grid-cols-[360px_1fr]"><section className="space-y-3">{people.map((person) => <button key={person.id} onClick={() => void open(person)} className={`w-full rounded-2xl border p-4 text-left ${selected?.id === person.id ? "border-primary bg-primary/5" : "border-border bg-card"}`}><div className="flex items-center gap-3"><Users size={18} className="text-primary" /><div><p className="font-semibold">{person.displayName}</p><p className="text-xs text-muted-foreground">{person.organizationalRole ?? person.roles?.join(", ") ?? "Relationship"}</p></div><span className="ml-auto rounded-full bg-muted px-2 py-1 text-[10px]">{person.relationshipHealth}</span></div><p className="mt-3 text-xs text-muted-foreground">Last contact: {person.lastInteractionAt ? new Date(person.lastInteractionAt).toLocaleDateString() : "Not recorded"}</p></button>)}{!people.length && <div className="rounded-2xl border border-dashed border-border p-8 text-center text-sm text-muted-foreground">No people recorded yet.</div>}</section>{intel ? <section className="rounded-2xl border border-border bg-card p-5"><div className="flex items-start justify-between"><div><p className="lee-label text-primary">Person brief</p><h2 className="mt-1 text-2xl font-semibold">{intel.person.displayName}</h2></div><div className="text-right"><p className="lee-label text-muted-foreground">Health</p><p className="mt-1 text-2xl font-semibold text-primary">{Math.round(intel.health.score)}<span className="text-sm">/100</span></p><p className="text-xs text-muted-foreground">{intel.health.momentum}</p></div></div><div className="mt-5 grid gap-3 sm:grid-cols-3"><div className="rounded-xl bg-muted/50 p-3"><p className="lee-label text-muted-foreground">Interactions</p><p className="mt-1 text-lg font-semibold">{intel.interactions.length}</p></div><div className="rounded-xl bg-muted/50 p-3"><p className="lee-label text-muted-foreground">Open promises</p><p className="mt-1 text-lg font-semibold">{intel.promises.filter((p: any) => p.status === "open").length}</p></div><div className="rounded-xl bg-muted/50 p-3"><p className="lee-label text-muted-foreground">Open questions</p><p className="mt-1 text-lg font-semibold">{intel.questions.filter((q: any) => q.status === "open").length}</p></div></div><div className="mt-5 rounded-xl border border-border p-4"><p className="lee-label text-primary">Promises</p><div className="mt-3 space-y-2">{intel.promises.map((item: any) => <div key={item.id} className="flex justify-between gap-3 text-sm"><span>{item.statement}</span><span className="text-xs text-muted-foreground">{item.direction} · {item.status}</span></div>)}{!intel.promises.length && <p className="text-sm text-muted-foreground">No promises tracked.</p>}</div><div className="mt-3 flex gap-2"><input value={text} onChange={(e) => setText(e.target.value)} placeholder="Add an outstanding promise…" className="h-9 flex-1 rounded-lg border border-input bg-background px-3 text-xs" /><button onClick={() => void addPromise()} className="rounded-lg bg-primary px-3 text-xs font-semibold text-primary-foreground"><Plus className="mr-1 inline" size={13} />Add</button></div></div><div className="mt-5"><p className="lee-label text-primary">Interaction timeline</p><div className="mt-3 space-y-3">{intel.interactions.slice(0, 8).map((item: any) => <div key={item.id} className="flex gap-3 border-b border-border pb-3 text-sm"><MessageCircle size={15} className="mt-0.5 text-primary" /><div><p>{item.summary}</p><p className="text-xs text-muted-foreground">{new Date(item.occurredAt).toLocaleDateString()} · {item.direction}</p></div></div>)}{!intel.interactions.length && <p className="text-sm text-muted-foreground">No interactions recorded.</p>}</div></div></section> : <div className="flex min-h-72 items-center justify-center rounded-2xl border border-dashed border-border text-sm text-muted-foreground">Select a person to open relationship intelligence.</div>}</div></div>;
}
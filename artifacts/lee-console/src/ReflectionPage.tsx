import { useEffect, useState } from "react";
import { BarChart3, RefreshCw } from "lucide-react";
export default function ReflectionPage() {
  const [items, setItems] = useState<any[]>([]); const [notice, setNotice] = useState("");
  const load = async () => { const r = await fetch("/api/reflections", { cache: "no-store" }); if (r.ok) setItems(await r.json()); }; useEffect(() => { void load(); }, []);
  const generate = async () => { const r = await fetch("/api/reflections/generate", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify({ period: "current", reportType: "weekly" }) }); setNotice(r.ok ? "Reflection generated from current ledger metrics." : "Reflection failed."); await load(); };
  return <div className="mx-auto max-w-[1100px]"><div className="flex items-end justify-between gap-3"><div><p className="lee-label text-primary">Growth history</p><h1 className="mt-2 text-3xl font-semibold">Reflections</h1><p className="mt-2 text-sm text-muted-foreground">Evidence-backed measurement of how the operating environment changes over time.</p></div><button onClick={() => void generate()} className="rounded-xl bg-primary px-3.5 py-2.5 text-xs font-semibold text-primary-foreground"><RefreshCw className="mr-2 inline" size={14} />Generate reflection</button></div>{notice && <p className="mt-4 rounded-xl border border-primary/25 bg-primary/10 px-4 py-3 text-sm text-primary">{notice}</p>}<div className="mt-5 space-y-4">{items.map((item) => <div key={item.id} className="rounded-2xl border border-border bg-card p-5"><div className="flex items-center gap-3"><BarChart3 className="text-primary" size={18} /><div><p className="lee-label text-primary">{item.reportType} · {item.period}</p><p className="text-xs text-muted-foreground">{new Date(item.generatedAt).toLocaleString()}</p></div></div><p className="mt-5 text-sm leading-relaxed">{item.narrative}</p><div className="mt-4 grid gap-2 sm:grid-cols-3">{Object.entries(item.dimensions).map(([key, value]) => <div key={key} className="rounded-xl bg-muted/50 p-3"><p className="lee-label text-muted-foreground">{key.replaceAll(/([A-Z])/g, " $1")}</p><p className="mt-1 text-lg font-semibold">{typeof value === "number" && key === "modelCostUsd" ? `$${Number(value).toFixed(4)}` : String(value)}</p></div>)}</div></div>)}{!items.length && <div className="rounded-2xl border border-dashed border-border p-10 text-center text-sm text-muted-foreground">No reflection reports yet.</div>}</div></div>;
}